include ../../header

html
    head
        meta(property='og:title'       content='GLOSSARY')
        meta(property='og:description' content='Terms Index')
        meta(property='og:url'         content='https://laurent.groupoid.space/mathematics/glossary/')

block title
    title GLOSSARY

block content
    nav
        <a href='../../index.html'>LAURENT</a>
        <a href='#'>GLOSSARY</a>
    article.main
        .exe
            section
                h1 GLOSSARY OF TERMS
                span.
        aside
            time Published: 21 MAR 2025
        .exe
            section
                h2 Terms
                dl
                    dt \(\forall\)
                    dd Universal quantifier: \(\forall x : A, P(x)\). See Forall.

                    dt \(\exists\)
                    dd Existential quantifier: \(\exists x : A, P(x)\). See Exists.

                    dt \(\text{Set}\)
                    dd Type of sets over a base type: \(\text{Set}(A) = \{ x : A \mid P(x) \}\). See Set.

                    dt \(\text{SetEq}\)
                    dd Set equality: \(S =_{\text{Set}(A)} T \iff \forall x : A, S(x) \iff T(x)\). See SetEq.

                    dt \(\mu\)
                    dd Measure constructor: \(\mu(A, \Sigma, f) : \text{Measure}(A, \Sigma)\). See Mu.

                    dt \(\text{Measure}\)
                    dd Measure type: \(\text{Measure}(A, \Sigma)\) for measurable spaces. See Measure.

                    dt \(\text{Seq}\)
                    dd Sequence type: \(\text{Seq}(A) = \mathbb{N} \rightarrow A\). See Seq.

                    dt \(\sup\)
                    dd Supremum: \(\sup S = \text{least upper bound of } S \subseteq \mathbb{R}\). See Sup.

                    dt \(\inf\)
                    dd Infimum: \(\inf S = \text{greatest lower bound of } S \subseteq \mathbb{R}\). See Inf.

                    dt \(\text{Lebesgue}\)
                    dd Lebesgue integral: \(\int_S f \, d\mu\). See Lebesgue.

                    dt \(\text{Limit}\)
                    dd Sequence limit: \(\lim_{n \to \infty} a_n = l\). See Limit.

                    dt \(\bigcup\)
                    dd Infinite union: \(\bigcup_{i : I} S_i\). See Union.

                    dt \(\bigcap\)
                    dd Intersection: \(S \cap T = \{ x \mid S(x) \land T(x) \}\). See Intersect.

                    dt \(\setminus\)
                    dd Complement: \(A \setminus S = \{ x : A \mid \neg S(x) \}\). See Complement.

                    dt \(\overline{S}\)
                    dd Closure: \(\overline{S} = S \cup \{ \text{limit points of } S \}\). See Closure.

                    dt \(\sum\)
                    dd Infinite sum: \(\sum_{i : I} a_i = \lim_{n} \sum_{k=0}^{n-1} a_k\). See Sum.

                    dt \(\land\)
                    dd Logical conjunction: \(P \land Q\). See And.

                    dt \(\lor\)
                    dd Logical disjunction: \(P \lor Q\). See Or.

                    dt \(\text{Prop}\)
                    dd Propositional universe: \(\text{Prop} = \{ P \mid P \text{ is 0-truncated} \}\). See Prop.

                    dt \(\mathcal{P}\)
                    dd Power set: \(\mathcal{P}(A) = \text{Set}(\text{Set}(A))\). See Power.

                    dt \(U_0, U_1\)
                    dd Type universes: \(U_0 : U_1\). See Universe.

                    dt \(\mathbb{R}\)
                    dd Real numbers: \(\mathbb{R} : U_0\) with limit axioms. See Real.

include ../../footer
